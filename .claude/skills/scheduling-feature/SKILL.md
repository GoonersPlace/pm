# Skill: scheduling-feature

Read `SRS.md`, `GRAPH_MODEL.md`, `SCHEDULING_RULES.md`, and `algorithm-state.md`. Preserve hard constraints. Keep the pipeline graph -> coloring -> time-slot mapping -> room assignment -> validation -> scoring explicit. Add tests for both valid and infeasible cases. Any change to core strategy or constraint semantics requires approval.
