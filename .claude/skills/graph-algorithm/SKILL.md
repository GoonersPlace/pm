# Skill: graph-algorithm

Graph algorithm changes are high-impact. Current approved algorithms are deterministic Greedy baseline and DSATUR improvement. Do not replace or materially alter their behavior without approval. Maintain the coloring invariant on every edge, deterministic tie-breaks, explainable implementation, and algorithm-specific tests/benchmarks.