# Skill: log-task

After a completed implementation task, create `docs/process/YYYY-MM-DD_<short-name>.md` containing: task/requirement, scope, files changed, decisions, commands run, tests/checks, result, known issues, and follow-ups. Never log secret values. Then update `.claude/memory/` only with durable project facts that changed. Do not claim checks passed unless they were actually run.
