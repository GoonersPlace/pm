# Skill: constraint-rule

Read `SCHEDULING_RULES.md`. Adding/removing/changing a hard or soft constraint requires owner approval. Hard constraints invalidate schedules; they must never be downgraded into score penalties silently. Soft rules must expose named/configurable weights and a score breakdown. Add focused tests.
