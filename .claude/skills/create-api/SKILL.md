# Skill: create-api

Before implementing an endpoint, read `docs/API.md` and relevant SRS requirement. Public contract changes require approval. Use `/api/v1`, validate input, return consistent envelopes, keep domain logic out of route handlers, and never expose stack traces/SQL/secrets. Add tests for validation and core behavior.
