# Task Logs

Completed implementation tasks are recorded here by the `log-task` skill.

Filename convention:

```text
YYYY-MM-DD_<short-task-name>.md
```

Each log records requirement/scope, files changed, decisions, commands/checks, results, known issues, and follow-ups. Logs must never contain `.env` secret values.
