#include <Course.h>
#include <Graph.h>
#include <Graph.cpp>
#include <iostream>
using namespace std;
int main(){
  AddEdge(G, 0, 1);
  AddEdge(G, 0, 2);
  AddEdge(G, 0, 3);
  AddEdge(G, 1, 2);
  AddEdge(G, 1, 4);
  AddEdge(G, 2, 3);
  AddEdge(G, 3, 4);
}
